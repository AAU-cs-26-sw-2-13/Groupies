# Groupies app (Node, native JS and HTML/CSS Application with an SQL database layer (MySQL))

## Requirements

## Setup
- Needs a running MySQL server
- Configure the .env to select the MySQL server with the correct credentials.

        MYSQL_HOST=
        MYSQL_PORT=
        MYSQL_USER=
        MYSQL_PASSWORD=
        MYSQL_DATABASE=

        PORT=
        NODE_ENV=development
- 